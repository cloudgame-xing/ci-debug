name: CI
on: [push]
jobs:
  build:
    runs-on: windows-11-arm
    steps:
      - name: check env
        shell: pwsh
        run: |
          cd "C:\Program Files\Microsoft Visual Studio\"
          Get-ChildItem -Recurse -Depth 100 -Name
